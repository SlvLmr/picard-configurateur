[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so React Router handles /admin/* on direct access / refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
